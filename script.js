// Create test objects
const student1 = {
  name: "John",
  age: 20,
  city: "New York"
};

const student2 = {
  name: "Jane"
};

// Function to get all keys from the object
function getKeys(obj) {
  return Object.keys(obj);
}